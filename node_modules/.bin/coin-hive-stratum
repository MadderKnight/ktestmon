#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../coin-hive-stratum/bin/coin-hive-stratum" "$@"
  ret=$?
else 
  node  "$basedir/../coin-hive-stratum/bin/coin-hive-stratum" "$@"
  ret=$?
fi
exit $ret
